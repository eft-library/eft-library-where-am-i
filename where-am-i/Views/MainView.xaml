<Window x:Class="where_am_i.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:where_am_i.ViewModels"
        mc:Ignorable="d"
        Title="EFT Library - 내 위치 찾기"
        Height="430"
        Width="460"
        ResizeMode="NoResize">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="32">
        <Border Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="10"
                Padding="28">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Image Grid.Row="0"
                       Width="190"
                       Height="54"
                       Margin="0,0,0,16"
                       Source="https://image.eftlibrary.com/eftlibrary/tkl_main%2Flogo.png"
                       Stretch="Uniform"/>

                <StackPanel Grid.Row="1" Margin="0,0,0,22">
                    <TextBlock Text="내 위치 찾기"
                               FontSize="22"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="EFT Library 계정으로 연결하세요"
                               Margin="0,6,0,0"
                               Foreground="{StaticResource SecondaryTextBrush}"
                               FontSize="13"
                               HorizontalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="0,0,0,16">
                    <TextBlock Text="이메일"
                               Margin="0,0,0,7"
                               FontSize="13"
                               FontWeight="SemiBold"/>
                    <TextBox Height="44"
                             Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <Button Grid.Row="3"
                        Content="사용자 확인"
                        Command="{Binding CheckUserCommand}"
                        Style="{StaticResource PrimaryButtonStyle}"
                        IsDefault="True"/>

                <TextBlock Grid.Row="4"
                           Margin="0,12,0,0"
                           Foreground="#FF6568"
                           FontSize="12"
                           TextAlignment="Center"
                           TextWrapping="Wrap"
                           Text="{Binding Message}"/>
            </Grid>
        </Border>
    </Grid>
</Window>
