<Window x:Class="where_am_i.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:where_am_i.Views"
        xmlns:vm="clr-namespace:where_am_i.ViewModels"
        mc:Ignorable="d"
        Title="사용자 확인" Height="300" Width="350">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 상단 로고 -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,20">
            <Image Width="200" Height="60">
                <Image.Source>
                    <BitmapImage UriSource="https://image.eftlibrary.com/eftlibrary/tkl_main%2Flogo.png"/>
                </Image.Source>
            </Image>
        </StackPanel>

        <!-- 이메일 입력 -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="이메일"
               FontWeight="Bold"
               FontSize="14"
               Margin="0,0,0,5"/>
            <TextBox Height="40"
             FontSize="14"
             Padding="8"
             Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
             BorderBrush="#CCC"
             BorderThickness="1"
             Background="White"/>
        </StackPanel>

        <!-- 확인 버튼 -->
        <Button Grid.Row="2"
        Height="40"
        Margin="0,0,0,10"
        Content="사용자 확인"
        Command="{Binding CheckUserCommand}"
        Background="#4CAF50"
        Foreground="White"
        FontWeight="SemiBold"
        FontSize="14"
        BorderThickness="0"
        HorizontalAlignment="Stretch"
        IsDefault="True"/>

        <!-- 메시지 -->
        <TextBlock Grid.Row="3"
           Margin="0,10,0,0"
           Foreground="Red"
           FontWeight="Bold"
           Text="{Binding Message}"/>
    </Grid>
    
</Window>
